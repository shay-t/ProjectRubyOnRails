<%= form_with(model: auteur) do |form| %>
  <% if auteur.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(auteur.errors.count, "error") %> prohibited this auteur from being saved:</h2>

      <ul>
        <% auteur.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom_prenom, style: "display: block" %>
    <%= form.text_field :nom_prenom %>
  </div>

  <div class="mt-3" >
    <%= form.submit class: "btn btn-outline-primary" %>
  </div>
<% end %>
