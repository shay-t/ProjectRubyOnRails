
<div class="mt-5">
  <%= form_with(model: adherent) do |form| %>
    <% if adherent.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(adherent.errors.count, "error") %> prohibited this adherent from being saved:</h2>

        <ul>
          <% adherent.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  <div class="mb-4">
      <div class="input-group mb-3">
        <div class="input-group-prepend">
            <%= form.label :nom, class: "input-group-text"  %>
          <%= form.text_field :nom, class: "form-control" %>
        </div>
      </div>

      <div class="input-group mb-3">
        <div class="input-group-prepend">
            <%= form.label :prenom, class: "input-group-text"  %>
          <%= form.text_field :prenom, class: "form-control" %>
        </div>
      </div>
  </div>

      <%= form.submit class: "btn btn-outline-primary" %>
  <% end %>
</div>

